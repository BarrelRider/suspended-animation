import * as React from "react";
import { Button } from "@suspended-animation/suspended-components";

export default {
  title: "Components",
  parameters: {
    info: { inline: true },
  },
};

export const ButtonStory = () => <Button />;
